[project]
name = "llm-config-sdk"
version = "0.1.0"
description = "大模型配置中心 Python SDK"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
openai = ["openai>=1.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

